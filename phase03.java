public class phase03 {

    package chat.example;

import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

    class phase03 {

        editUserProfile = verificationModification();

        @Test
        void editUserProfile() {
        }

        @Test
        void editPassword() {
            assertTrue(editUserProfile.Enabled());
            assertFalse(editUserProfile.Enabled());
        }
        @Test
        void editEmail() {
            assertTrue(editEmail.check());
            assertFalse(editEmail.check());
        }

    }

}
